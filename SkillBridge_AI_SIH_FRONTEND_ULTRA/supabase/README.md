# Supabase backend
The live SkillBridge project was upgraded with the enterprise product foundation migration. `schema_legacy.sql` is the original prototype schema snapshot. For deployment to a new project, recreate the base schema first, then apply the enterprise migration from your Supabase migration history.
